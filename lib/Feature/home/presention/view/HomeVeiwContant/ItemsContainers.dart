import 'package:flutter/cupertino.dart';

class ItemsContainers extends StatelessWidget {
  const ItemsContainers({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      // width: ,
    );
  }
}
